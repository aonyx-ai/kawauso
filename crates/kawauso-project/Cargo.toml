[package]
name = "kawauso-project"
description = "Projects for Kawauso applications"

version = "0.0.0"

edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

documentation = "https://docs.rs/kawauso-project"
readme = "README.md"

categories = ["development-tools", "filesystem"]
keywords = ["project", "discovery", "configuration"]

include = [
  "CHANGELOG.md",
  "LICENSE-APACHE",
  "LICENSE-MIT",
  "README.md",
  "src/**/*.rs",
  "tests/*.rs",
]

[dependencies]
bon = { workspace = true }
kawauso-config = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }
typed-fields = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
